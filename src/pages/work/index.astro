---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/sections/PageHero.astro';
import ProjectCard from '../../components/cards/ProjectCard.astro';

const projects = (await getCollection('work')).sort((a, b) => a.data.order - b.data.order);
---

<BaseLayout
  title="Work — Sarah G. Randol"
  description="Completed analytics, optimization, machine learning, and mathematical modeling projects — evidence of capability, not claims."
>
  <PageHero
    eyebrow="Work"
    title="Evidence, not claims"
    description="Completed analytics, optimization, machine learning, and dashboard projects. Each one is real, finished work — methodology, data, and results included."
    motif="optimization"
  />

  <section class="section section--tight work-intro">
    <div class="container work-intro__grid">
      <p class="work-intro__lede">
        Every project below shipped against a real dataset and a real constraint — not a tutorial exercise.
      </p>
      <p class="text-muted work-intro__meta">
        {projects.length} completed projects spanning optimization, applied AI, and decision analytics.
      </p>
    </div>
  </section>

  <section class="section section--tight">
    <div class="container">
      <div class="work-grid">
        {projects.map((project) => (
          <ProjectCard
            slug={project.id}
            title={project.data.title}
            summary={project.data.summary}
            methods={project.data.methods}
            status={project.data.status}
            thumbnail={project.data.thumbnail}
            video={project.data.video}
          />
        ))}
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .work-intro__grid {
    display: grid;
    gap: var(--space-4);
    padding-bottom: var(--space-8);
    border-bottom: 1px solid var(--border);
  }

  .work-intro__lede {
    font-family: var(--font-serif);
    font-size: var(--text-lg);
    line-height: var(--leading-snug);
    max-width: 32rem;
  }

  .work-intro__meta {
    font-size: var(--text-sm);
    max-width: 20rem;
  }

  .work-grid {
    display: grid;
    gap: var(--space-6);
    grid-template-columns: repeat(auto-fit, minmax(18rem, 1fr));
  }

  @media (min-width: 720px) {
    .work-intro__grid {
      grid-template-columns: 1fr auto;
      align-items: end;
    }
  }
</style>
